{
  "name": "yagal",
  "version": "0.0.0",
  "description": "Yet-Another Generative Art Language",
  "scripts": {
    "build-grammar": "pegjs src/grammar.pegjs",
    "test": "npm run build-grammar && node src/cli.js test.ygl out.png",
    "build-editor": "npm run build-grammar && webpack --config webpack-editor.config.js"
  },
  "author": "",
  "license": "WTFPL",
  "dependencies": {
    "canvas": "^2.6.0",
    "pegjs": "^0.10.0"
  },
  "devDependencies": {
    "webpack": "^4.41.2",
    "webpack-cli": "^3.3.12"
  }
}
